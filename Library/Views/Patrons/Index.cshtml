@using Library.Models;

<h1>Patron Home</h1>

@if(Model.Count !=0)
{
    <ul>
    @foreach(Patron patron in Model)
    {
        <li><a href="/patrons/@patron.GetId()">@patron.GetName()</li>
    }
    </ul>
}
<p><a href="/patrons/new">Add patron</a></p>
<p><a href="/">Home</a></p>

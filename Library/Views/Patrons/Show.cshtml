@using Library.Models;
@using System.Collections.Generic;

<h1>@Model["patron"].GetName()</h1>

@for(int i=0; i < Model["books"].Count; i++)
{
    <h4>@Model["books"][i]["book"].GetTitle()</h4>
    @for(int j=0; j<Model["books"][i]["authors"].Count; j++)
    //foreach(Author author in Model["books"][i]["authors"])
    {
        <p>@Model["books"][i]["authors"][j].GetName()</p>
        //<p>author.GetName()</p>
    }
   
}

<p><a href='/patrons/@Model["patron"].GetId()/checkout'>Checkout a book</a></p>
<p><a href='/patrons/@Model["patron"].GetId()/edit'>Edit Patron</a></p>
<p><a href="/patrons">Back to patrons</a></p>
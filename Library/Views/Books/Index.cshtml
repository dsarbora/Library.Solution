@using Library.Models;
<h1>Books</h1>
@if(Model.Count == 0)
{
    <p>No books yet</p>
}
@if(Model.Count != 0)
{
    @foreach(Book book in Model)
    {
        <p><a href="/books/@book.GetId()">@book.GetTitle()</a></p>
    }
}
<a href="/">Go home</a>
<a href="/books/new">Add new book</a>